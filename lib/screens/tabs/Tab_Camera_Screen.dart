import 'package:flutter/material.dart';

class camera_screen extends StatelessWidget {
  camera_screen({super.key, });
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("camera_screen"));
  }
}